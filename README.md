# sql_data_warehouse
Data warehouse project built on MS SQL Server, implementing Medallion architecture (Bronze–Silver–Gold layers) with a robust ETL pipeline to ingest, transform, and deliver analytics-ready data.
